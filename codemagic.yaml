workflows:
  android-apk:
    name: Build Android APK
    instance_type: mac_mini_m1
    max_build_duration: 30
    environment:
      flutter: stable
      java: 17
    scripts:
      - name: 1. Generate Android & Perfect Permission Injection
        script: |
          flutter create . --org com.laxmitrading.app --platforms=android
          
          # यह पाइथन स्क्रिप्ट 100% गारंटी के साथ सही जगह इंटरनेट परमिशन जोड़ देगी
          python3 -c '
          import os
          path = "android/app/src/main/AndroidManifest.xml"
          if os.path.exists(path):
              with open(path, "r") as f:
                  content = f.read()
              if "android.permission.INTERNET" not in content:
                  content = content.replace("<application", "<uses-permission android:name=\"android.permission.INTERNET\"/>\n    <application")
                  with open(path, "w") as f:
                      f.write(content)
                  print("SUCCESS: Internet permission added!")
              else:
                  print("ALREADY EXISTS: Internet permission is already there.")
          else:
              print("ERROR: AndroidManifest.xml not found!")
          '
      - name: 2. Get Flutter packages
        script: |
          flutter pub get
      - name: 3. Build Android APK
        script: |
          flutter build apk --release --no-tree-shake-icons
    artifacts:
      - build/app/outputs/flutter-apk/*.apk
      
